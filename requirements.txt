# 核心框架（版本对齐实际运行环境：conda env "bot"，Python 3.10）
# 注意：pydantic 必须保持 v1（hikari-core 钉死 <2.0），fastapi 不可升过 0.141（强制 pydantic>=2.9）
nonebot2==2.5.0
nonebot-adapter-onebot==2.4.6
nonebot-plugin-apscheduler==0.2.0
fastapi==0.110.1
uvicorn==0.29.0
websockets==16.1.1

# 加载的外部插件
hikari-bot==1.0.9
nonebot-plugin-send-anything-anywhere==0.6.0
nonebot-plugin-htmlrender==0.5.0

# 项目依赖
httpx==0.27.2
aiohttp==3.13.3
aiofiles==24.1.0
requests==2.31.0
PyYAML==6.0.1
pydantic==1.10.26
loguru==0.7.2
APScheduler==3.10.4
Pillow==9.5.0
psutil==6.1.0
tiktoken==0.12.0
jieba==0.42.1
Markdown==3.7
typing_extensions==4.15.0
playwright==1.49.0
trafilatura==2.1.0

# 可选依赖（未安装时对应功能自动降级或关闭）：
#   ujson                        # JSON 加速（未安装时回退标准库 json）
#   numpy                        # text_func 记忆增强（遗留功能，未安装自动关闭）
#   nonebot-plugin-gocqhttp      # InlineGoCQHTTP 内置协议端（需 config.yml 中 enabled: true）
